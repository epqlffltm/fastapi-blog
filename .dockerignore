# 빌드 컨텍스트에서 뺄 것들.
# 여기 안 적으면 데몬으로 통째로 전송돼 빌드가 느려지고, 최악의 경우
# .env 나 로컬 DB 가 이미지 안에 박힌다.

.git
.github
.gitignore
.gitattributes

# 로컬 가상환경 — 컨테이너 안에서 다시 만든다
.venv
venv
env

__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# 비밀값은 이미지가 아니라 실행 시점에 주입한다
.env
.env.*
!.env.example

# 로컬 DB
*.db
*.sqlite
*.sqlite3

# 사용자 업로드 — 런타임 데이터. 볼륨으로 붙인다
static/img/*
!static/img/.gitkeep

.vscode/
.idea/
.DS_Store
Thumbs.db
