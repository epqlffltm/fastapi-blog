# 두 대 이상의 기계를 오갈 때 줄바꿈 설정이 달라 파일 전체가 수정된 것처럼
# 보이는 diff 를 막는다. 저장소에는 항상 LF 로 저장한다.
* text=auto eol=lf

# 이진 파일은 변환하지 않는다
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.webp binary
*.ico  binary
